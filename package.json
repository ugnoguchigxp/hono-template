{
  "name": "hono-template",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --build",
    "test": "vitest",
    "test:run": "vitest run",
    "lint": "tsc --noEmit",
    "db:generate": "pnpm --filter @foundation/db generate",
    "db:migrate": "pnpm --filter @foundation/db migrate",
    "db:studio": "pnpm --filter @foundation/db studio"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.2.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "packageManager": "pnpm@8.14.0"
}
